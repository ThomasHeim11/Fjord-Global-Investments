# Python dependencies for the FastAPI backend (run from the repo root):
#   pip install -r requirements.txt
fastapi>=0.115
uvicorn>=0.30
anthropic>=0.84
sentence-transformers>=2.7
faiss-cpu>=1.8
pypdf>=4.0
python-dotenv>=1.0
pydantic>=2.0
groq

# Tests
pytest>=7.4
httpx>=0.27          # FastAPI TestClient transport
