# Test dependencies (the app itself needs only requirements.txt).
#   pip install -r requirements-dev.txt
#   pytest
-r requirements.txt
pytest>=7.4
httpx>=0.27          # FastAPI TestClient transport
